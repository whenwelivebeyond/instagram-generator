// This page is intentionally standalone while the scheduler is being built.
// Future milestones will add the secure queue and Instagram connection here.
const queueCount = document.querySelector("#queueCount");
queueCount.textContent = "0 posts";
